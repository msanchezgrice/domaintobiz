<nav class="bg-white shadow-sm border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <!-- Logo and brand -->
            <div class="flex items-center">
                <a href="/" class="flex items-center space-x-2">
                    <div class="w-8 h-8 bg-{{ brand.primaryColor | default('#3B82F6') }} rounded-lg flex items-center justify-center">
                        <span class="text-white font-bold text-sm">{{ brand.name[0] | upper }}</span>
                    </div>
                    <span class="font-bold text-xl text-gray-900">{{ brand.name }}</span>
                </a>
            </div>
            
            <!-- Desktop Navigation -->
            <div class="hidden md:flex items-center space-x-8">
                <a href="/" class="text-gray-700 hover:text-{{ brand.primaryColor | default('blue-600') }} px-3 py-2 text-sm font-medium transition-colors">
                    Home
                </a>
                {% if modules.pricing %}
                <a href="/pricing" class="text-gray-700 hover:text-{{ brand.primaryColor | default('blue-600') }} px-3 py-2 text-sm font-medium transition-colors">
                    Pricing
                </a>
                {% endif %}
                {% if modules.waitlist %}
                <a href="/waitlist" class="text-gray-700 hover:text-{{ brand.primaryColor | default('blue-600') }} px-3 py-2 text-sm font-medium transition-colors">
                    Join Waitlist
                </a>
                {% endif %}
                {% if modules.dashboard %}
                <a href="/dashboard" class="text-gray-700 hover:text-{{ brand.primaryColor | default('blue-600') }} px-3 py-2 text-sm font-medium transition-colors">
                    Dashboard
                </a>
                {% endif %}
            </div>
            
            <!-- CTA Button -->
            <div class="hidden md:flex">
                {% if modules.stripePaywall %}
                <button 
                    hx-post="/api/pay" 
                    hx-swap="outerHTML"
                    class="bg-{{ brand.primaryColor | default('blue-600') }} text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-{{ brand.primaryColor | default('blue-700') }} transition-colors"
                >
                    Get Started
                </button>
                {% else %}
                <a href="/waitlist" class="bg-{{ brand.primaryColor | default('blue-600') }} text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-{{ brand.primaryColor | default('blue-700') }} transition-colors">
                    Get Started
                </a>
                {% endif %}
            </div>
            
            <!-- Mobile menu button -->
            <div class="md:hidden">
                <button 
                    type="button" 
                    class="text-gray-700 hover:text-gray-900 focus:outline-none focus:text-gray-900"
                    hx-get="/partials/mobile-menu" 
                    hx-target="#mobile-menu" 
                    hx-swap="outerHTML"
                >
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>
    </div>
    
    <!-- Mobile menu (hidden by default) -->
    <div id="mobile-menu" class="md:hidden"></div>
</nav> 